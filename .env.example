# Скопируй в .env и вставь ключи своего проекта Supabase
# (Supabase → Project Settings → API). Без них приложение
# работает в демо-режиме на localStorage этого браузера.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
